# ============================================================
# Frontend — Variáveis de Ambiente (copie para .env se necessário)
# ============================================================

# URL base da API do backend.
# Em desenvolvimento: deixe vazio (o proxy do CRA redireciona /api/* para :3000).
# Em produção (Vercel): defina a URL do backend, ex: https://gpi-nmnp.onrender.com
# REACT_APP_API_URL=
